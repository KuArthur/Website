link(rel="stylesheet", href="index.scss")
mixin like(check)
    .checkbox
        label.checkbox__wrap
            if check
                input(type="checkbox" checked)
            else
                input(type="checkbox")
            span.checkbox__like
        //- span.checkbox__like_text.typo.typo_theme_default.typo_normal=text